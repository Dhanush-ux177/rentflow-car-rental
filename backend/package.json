{
  "name": "backend",
  "version": "1.0.0",
  "description": "RentFlow Car Rental Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seed.js",
    "build": "npm install && cd ../frontend && npm install && npm run build && cd ../backend && rm -rf public && mkdir public && cp -r ../frontend/dist/* public/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongod": "^2.0.0",
    "mongoose": "^9.9.1",
    "multer": "^2.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}